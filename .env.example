# This file serves as an example for the required environment variables.
# For local development, copy this file to .env.local and populate it with
# your credentials, ideally by using `vercel env pull`.

# Common
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Analytics & Monitoring
# Sentry for error monitoring (can be public but env-specific)
# SENTRY_DSN=your_sentry_dsn_here
# PostHog for product analytics (client-safe, using EU instance for compliance)
# NEXT_PUBLIC_POSTHOG_KEY=phc_your_posthog_key
# NEXT_PUBLIC_POSTHOG_HOST="https://eu.posthog.com"

# Supabase
# These are populated by `supabase start` for local dev or from Vercel for remote envs.
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=
# DATABASE_URL=

# Supabase Project References (for CLI linking)
# Used by `pnpm db:link:*` scripts. Get these from your Supabase project dashboard.
# SUPABASE_STAGING_PROJECT_REF=
# SUPABASE_PROD_PROJECT_REF=

# Sanity CMS Configuration
## For the Next.js app (client-safe)
# NEXT_PUBLIC_SANITY_PROJECT_ID=your_sanity_project_id
# NEXT_PUBLIC_SANITY_DATASET="development"
## For the embedded Sanity Studio (client-safe, forwarded to Vite)
# SANITY_STUDIO_PROJECT_ID=your_sanity_project_id
# SANITY_STUDIO_DATASET="development"
## Server-only read token (NEVER expose to client or Studio)
# SANITY_API_READ_TOKEN=your_sanity_read_token

# Stripe Payment Processing
# Use test keys (pk_test_*, sk_test_*) for development
# STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
# STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Mux Video Streaming
# Token ID and Secret for API access (both server-only)
# For Studio (Vite) exposure, prefix with SANITY_STUDIO_ to make them available to the plugin at build time.
# Never commit real values.
# MUX_TOKEN_ID=your_mux_token_id
# MUX_TOKEN_SECRET=your_mux_token_secret
# SANITY_STUDIO_MUX_TOKEN_ID=your_mux_token_id
# SANITY_STUDIO_MUX_TOKEN_SECRET=your_mux_token_secret

# Email Services
# Resend for transactional emails (server-only)
# RESEND_API_KEY=re_your_resend_api_key

# MailerLite for marketing automation (server-only)
# MAILERLITE_API_KEY=your_mailerlite_api_key
